# Redis Connection
# For local developm# Redis Connection (matches docker-compose.yml)
REDIS_PASSWORD="local_dev_password"
REDIS_URL="redis://:local_dev_password@localhost:6379"

# For Redis Cloud (production)
# REDIS_URL="redis://:your-password@your-host.cloud.redislabs.com:12345"

# Optional: Redis connection options
# REDIS_MAX_RETRIES=3
# REDIS_RETRY_DELAY=50
